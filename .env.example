# Supply through your shell or CI secrets; .env is not loaded automatically.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
OPENSEO_ACCESS_TOKEN=
# OPENSEO_API_KEY is also accepted; create it in OpenSEO Settings > API keys.
